<UserControl x:Class="PharmacyManagement.UI.Views.MedicineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Medicine Management" FontSize="28" FontWeight="Bold" 
                  Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,15"/>

        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                        Width="250" Height="35" FontSize="13"
                        Background="{DynamicResource InputBackgroundBrush}"
                        Foreground="{DynamicResource ForegroundBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        Padding="10,0" VerticalContentAlignment="Center"
                        Margin="0,0,10,0"/>
                <Button Content="Search" Style="{DynamicResource PrimaryButtonStyle}" 
                       Command="{Binding SearchCommand}" Height="35" Width="80" Margin="0,0,10,0"/>
                <Button Content="Clear" Style="{DynamicResource SecondaryButtonStyle}" 
                       Command="{Binding RefreshCommand}" Height="35" Width="80"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <ComboBox ItemsSource="{Binding Categories}" 
                         SelectedItem="{Binding SelectedCategory}" 
                         Width="150" Height="35" Margin="0,0,10,0"
                         Background="{DynamicResource InputBackgroundBrush}"
                         Foreground="{DynamicResource ForegroundBrush}"
                         BorderBrush="{DynamicResource BorderBrush}"
                         Padding="8,4"
                         ItemContainerStyle="{StaticResource AppComboBoxItemStyle}"
                         SelectionChanged="OnCategoryChanged">
                    <ComboBox.Style>
                        <Style TargetType="ComboBox">
                            <Setter Property="Focusable" Value="True" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ComboBox">
                                        <Grid>
                                            <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="4" SnapsToDevicePixels="True">
                                                <DockPanel Margin="4,0">
                                                    <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" VerticalAlignment="Center" />
                                                    <ToggleButton x:Name="ToggleButton" DockPanel.Dock="Right" Focusable="False" IsChecked="{Binding IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" Background="Transparent" BorderThickness="0" Width="22" HorizontalAlignment="Right">
                                                        <Path Data="M 0 0 L 4 4 L 8 0 Z" Fill="{DynamicResource ForegroundBrush}" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </ToggleButton>
                                                </DockPanel>
                                            </Border>
                                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                                <Border Background="{DynamicResource CardBackgroundBrush}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="4">
                                                    <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                                    </ScrollViewer>
                                                </Border>
                                            </Popup>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ComboBox.Style>
                    <ComboBox.Resources>
                        <!-- Ensure the popup background and text use the dark theme brushes -->
                        <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="{StaticResource CardBackgroundColor}"/>
                        <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="{StaticResource ForegroundColor}"/>
                    </ComboBox.Resources>
                </ComboBox>
                <Button Content="+ Add Medicine" Style="{DynamicResource SuccessButtonStyle}" 
                       Command="{Binding AddNewCommand}" Height="35" Width="120"/>
            </StackPanel>
        </Grid>

        <!-- Edit Form -->
        <Border Grid.Row="2" Background="{DynamicResource CardBackgroundBrush}" 
               BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" 
               CornerRadius="8" Padding="20" Margin="0,0,0,15"
               Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Medicine Details" FontSize="16" FontWeight="SemiBold" 
                          Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,15"/>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,10">
                        <TextBlock Text="Medicine Name *" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding NewMedicine.MedicineName}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,10,10">
                        <TextBlock Text="Barcode *" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding NewMedicine.Barcode}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,0,10">
                        <TextBlock Text="Category" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding NewMedicine.Category}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,10,10">
                        <TextBlock Text="Generic Name" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding NewMedicine.GenericName}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,10,10">
                        <TextBlock Text="Brand Name" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding NewMedicine.BrandName}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,0,0,10">
                        <TextBlock Text="Batch Number" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding NewMedicine.BatchNumber}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,10,10">
                        <TextBlock Text="Purchase Price" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding NewMedicine.PurchasePrice}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,0,10,10">
                        <TextBlock Text="Selling Price" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding NewMedicine.SellingPrice}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.Column="2" Margin="0,0,0,10">
                        <TextBlock Text="Quantity" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding NewMedicine.Quantity}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Row="3" Grid.Column="0" Margin="0,0,10,10">
                        <TextBlock Text="Min. Quantity" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding NewMedicine.MinimumQuantity}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Row="3" Grid.Column="1" Margin="0,0,10,10">
                        <TextBlock Text="Rack Number" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding NewMedicine.RackNumber}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Row="3" Grid.Column="2" Margin="0,0,0,10">
                        <TextBlock Text="Shelf Number" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding NewMedicine.ShelfNumber}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Row="4" Grid.Column="0" Margin="0,0,10,10">
                        <TextBlock Text="Supplier" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding NewMedicine.Supplier}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Row="4" Grid.Column="1" Margin="0,0,10,10">
                        <TextBlock Text="Expiry Date" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,3"/>
                        <DatePicker SelectedDate="{Binding NewMedicine.ExpiryDate}" Height="30"
                                    Foreground="{DynamicResource SecondaryForegroundBrush}"
                                    Background="{DynamicResource InputBackgroundBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"/>
                    </StackPanel>

                    <StackPanel Grid.Row="4" Grid.Column="2" Margin="0,0,0,10">
                        <CheckBox Content="Prescription Required" 
                                 IsChecked="{Binding NewMedicine.PrescriptionRequired}"
                                 Foreground="{DynamicResource ForegroundBrush}" 
                                 VerticalAlignment="Bottom" Margin="0,20,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="3" 
                               Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <!-- Show Save when not editing -->
                        <Button Content="Save" Style="{StaticResource SuccessButtonStyle}" 
                                Command="{Binding SaveCommand}" Height="35" Width="100" Margin="0,0,10,0"
                                Visibility="{Binding IsEditing, Converter={StaticResource InverseBooleanConverter}}"/>

                        <!-- Show Update when editing -->
                        <Button Content="Update" Style="{StaticResource SuccessButtonStyle}" 
                                Command="{Binding SaveCommand}" Height="35" Width="100" Margin="0,0,10,0"
                                Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                        <Button Content="Cancel" Style="{StaticResource SecondaryButtonStyle}" 
                               Command="{Binding CancelEditCommand}" Height="35" Width="100"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- Data Grid -->
        <Border Grid.Row="3" Background="{DynamicResource CardBackgroundBrush}" 
               BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" 
               CornerRadius="8" Padding="15">
            <DataGrid ItemsSource="{Binding Medicines}" SelectedItem="{Binding SelectedMedicine}"
                     AutoGenerateColumns="False" IsReadOnly="True" 
                     Background="Transparent" BorderThickness="0"
                     HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                     RowBackground="Transparent">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Barcode" Binding="{Binding Barcode}" Width="120"/>
                    <DataGridTextColumn Header="Medicine Name" Binding="{Binding MedicineName}" Width="200"/>
                    <DataGridTextColumn Header="Generic" Binding="{Binding GenericName}" Width="150"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120"/>
                    <DataGridTextColumn Header="Stock" Binding="{Binding Quantity}" Width="80"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding SellingPrice, StringFormat=C}" Width="100"/>
                    <DataGridTextColumn Header="Expiry" Binding="{Binding ExpiryDate, StringFormat=d}" Width="120"/>
                    <DataGridTextColumn Header="Location" Binding="{Binding ExactLocation}" Width="150"/>
                    <DataGridCheckBoxColumn Header="Rx" Binding="{Binding PrescriptionRequired}" Width="50"/>
                    <DataGridTemplateColumn Header="Actions" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Edit" Style="{DynamicResource SmallPrimaryButtonStyle}" 
                                           Command="{Binding DataContext.EditCommand, 
                                            RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                           CommandParameter="{Binding}" Margin="0,0,5,0"/>
                                    <Button Content="Del" Style="{DynamicResource SmallDangerButtonStyle}" 
                                           Command="{Binding DataContext.DeleteCommand, 
                                            RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                           CommandParameter="{Binding}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
